spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hermes
    username: postgres
    password: 123456


  jpa:
    show-sql: true

app:
  jwt:
    secret: banana

logging:
  level:
    root: INFO
    com.marcus.template: DEBUG
    org.springframework.security: DEBUG